/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { Async2Pipe } from './async2.pipe';

describe('Async2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Async2Pipe();
    expect(pipe).toBeTruthy();
  });
});
